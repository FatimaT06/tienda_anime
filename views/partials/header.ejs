<header class="header">
  <nav class="navbar">
    <div class="nav-container">
      <a href="/" class="logo">🎌 Tienda Anime</a>
      
      <div class="nav-links">
        <a href="/" class="nav-link">Productos</a>
        
        <% if (usuario) { %>
          <a href="/historial" class="nav-link">Mis Compras</a>
          <a href="/carrito" class="nav-link carrito-link">
            🛒 Carrito
            <span class="carrito-badge" id="carrito-count">
              <%= carrito.reduce((sum, item) => sum + item.cantidad, 0) %>
            </span>
          </a>
          <div class="user-menu">
            <span class="user-name">👤 <%= usuario.nombre %></span>
            <a href="/logout" class="nav-link logout-btn">Cerrar Sesión</a>
          </div>
        <% } else { %>
          <a href="/carrito" class="nav-link carrito-link">
            🛒 Carrito
            <span class="carrito-badge" id="carrito-count">
              <%= carrito.reduce((sum, item) => sum + item.cantidad, 0) %>
            </span>
          </a>
          <a href="/login" class="nav-link">Iniciar Sesión</a>
          <a href="/registro" class="nav-link btn-registro">Registrarse</a>
        <% } %>
      </div>
    </div>
  </nav>
</header>